<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pot Droid" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Pot Droid">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="app-shell">
      <header class="topbar">
        <div class="topbar-inner">
          <%= link_to root_path, class: "brand" do %>
            <span class="brand-mark">PD</span>
            <span class="brand-text">
              <span class="brand-title">PotDroid</span>
              <span class="brand-subtitle">Road defect operations</span>
            </span>
          <% end %>

          <% if authenticated? %>
            <% paired_device = Current.user.latest_paired_android_device %>
            <nav class="topbar-actions" aria-label="Primary">
              <% if paired_device %>
                <div class="device-status paired" title="Paired <%= l paired_device.claimed_at, format: :short %>">
                  <span class="status-dot"></span>
                  <span>Android paired</span>
                  <span class="device-name"><%= paired_device.device_name.presence || "Android device" %></span>
                </div>
              <% end %>
              <%= link_to "Candidates", candidate_potholes_path, class: "button ghost" %>
              <%= link_to "Map", map_candidate_potholes_path, class: "button ghost" %>
              <%= link_to "Reconcile", candidate_pothole_reconciliations_path, class: "button ghost" %>
              <%= link_to "Detector lab", new_detector_validation_path, class: "button ghost" %>
              <%= button_to "Pair Android app", pairing_sessions_path, class: "button primary" %>
              <%= button_to "Sign out", session_path, method: :delete, class: "button" %>
            </nav>
          <% end %>
        </div>
      </header>

      <% if Rails.env.development? %>
        <aside class="dev-banner" aria-label="Development seed credentials">
          <div class="dev-banner-inner">
            <span>Development seed login</span>
            <code><%= DevSeedCredentials::EMAIL %></code>
            <span>/</span>
            <code><%= DevSeedCredentials::PASSWORD %></code>
          </div>
        </aside>
      <% end %>

      <main class="app-main">
        <% if alert = flash[:alert] %>
          <div class="flash-stack">
            <p class="flash alert" id="alert"><%= alert %></p>
          </div>
        <% end %>

        <% if notice = flash[:notice] %>
          <div class="flash-stack">
            <p class="flash notice" id="notice"><%= notice %></p>
          </div>
        <% end %>

        <%= yield %>
    </main>
    </div>
  </body>
</html>
